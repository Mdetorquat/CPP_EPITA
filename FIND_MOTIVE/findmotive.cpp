#include <string.h>
#include <assert.h>
#include <stddef.h>

#include "findmotive.hpp"

int getLength(const char *str)
{
    int count = 0;
    while (str[count])
    {
        count++;
    }
    return count;
}


int myfindmotive(char* file, int count)
{
	int nb = 0;

	for(int i = 0; i < getLength(path); i++)
	{
		//TODO
	}

	return nb;
}
